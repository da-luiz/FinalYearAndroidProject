package com.example.finalyearproject.presentation.components

/*
import androidx.compose.foundation.clickable
import androidx.compose.foundation.layout.*
import androidx.compose.material3.Card
import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp
import com.example.finalyearproject.domain.models.Timetable

@Composable
fun TimetableCard(entry: Timetable, onClick: () -> Unit) {
    Card(
        modifier = Modifier
            .fillMaxWidth()
            .padding(8.dp)
            .clickable { onClick() }
    ) {
        Column(modifier = Modifier.padding(16.dp)) {
            Text(text = entry.subject, style = MaterialTheme.typography.headlineMedium)
            Text(text = entry.time, style = MaterialTheme.typography.bodyLarge)
            Text(text = entry.location, style = MaterialTheme.typography.bodySmall)
        }
    }
}
*/